@model List<TypeBlog>

<div class="card shadow border-0 my-4">
	<div class="card-header ml-0 py-3">
		<div class="row">
			<div class="col-12 text-center">
				<h2 class="text-primary py-2">TYPE BLOG LIST</h2>
			</div>
		</div>
		<div class="card-body">
			<div class="row pt-4 pb-3">
				<div class="col-6">
					
				</div>
				<div class="col-6 text-end">
					<a asp-controller="Blog" asp-action="Create" class="btn btn-primary">
						<i class="bi bi-plus-circle"></i> Create New
					</a>
				</div>
			</div>
			<table class="table table-bordered table-striped">
				<thead>
					<tr>
						<th>Name</th>
						<th>Desciption</th>
						<th>Actions</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var obj in Model.OrderBy(u => u.id_TypeBlog))
					{
						<tr>
							<td>@obj.Name</td>
							<td>@obj.Description</td>
							<td>
								<div class="w-75 btn-group" role="group">
									<a asp-controller="TypeBlog" asp-action="Edit" asp-route-id="@obj.id_TypeBlog" class="btn btn-primary mx-2">
										<i class="bi bi-pencil-square"></i>Edit
									</a>
									<a asp-controller="TypeBlog" asp-action="Delete" asp-route-id="@obj.id_TypeBlog" class="btn btn-danger mx-2">
										<i class="bi bi-trash-fill"></i>Delete
									</a>
								</div>
							</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>

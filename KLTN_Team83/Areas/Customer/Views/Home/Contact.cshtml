<!-- Contact Section -->
<section class="contact-wrapper">
    <div class="contact-header-section">
        <h1>🌿 Contact Us</h1>
        <p>We're here to assist you on your wellness journey. Reach out with any questions or inquiries!</p>
    </div>

    <section class="get-in-touch-section">
        <h2>Get In Touch</h2>
        <form class="contact-form" id="contactForm" method="post">
            <div class="form-group">
                <label for="firstName">First Name *</label>
                <input type="text" id="firstName" name="firstName" required aria-required="true" placeholder="Enter your first name">
            </div>
            <div class="form-group">
                <label for="lastName">Last Name *</label>
                <input type="text" id="lastName" name="lastName" required aria-required="true" placeholder="Enter your last name">
            </div>
            <div class="form-group">
                <label for="email">Email *</label>
                <input type="email" id="email" name="email" required aria-required="true" placeholder="example@domain.com">
            </div>
            <div class="form-group">
                <label for="subject">Subject</label>
                <input type="text" id="subject" name="subject" placeholder="Enter the subject">
            </div>
            <div class="form-group full-width">
                <label for="message">Message</label>
                <textarea id="message" name="message" rows="5" placeholder="Write your message here"></textarea>
            </div>
            <button type="submit" class="submit-button">Submit</button>
        </form>
        <div id="successMessage" class="success-message" style="display: none;">
            <p>✅ Thank you! Your message has been sent successfully.</p>
        </div>
    </section>

    <section class="contact-info-section">
        <div class="map-container">
            <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3834.3118980903823!2d108.1552256766271!3d16.04929670035895!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x31421938d61a3ce5%3A0x29d80f3ebbdcb44a!2zxJDhuqFpIEjhu41jIER1eSBUw6JuIEjDsmEgS2jDoW5oIE5hbQ!5e0!3m2!1svi!2s!4v1746692348855!5m2!1svi!2s" width="100%" height="400" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div>
        <div class="contact-details">
            <p>120 Hoàng Minh Thảo, Hoà Khánh Nam, Liên Chiểu, Đà Nẵng</p>
            <p>Phone: 123-456-7890</p>
            <p>Email: <a href="mailto:nhom83@gmail.com">nhom83@gmail.com</a></p>
        </div>
    </section>
</section>

<script>
    document.getElementById('contactForm').addEventListener('submit', function(event) {
        event.preventDefault(); // Prevent default form submission for demo

        // Simulate form submission (replace with actual backend call)
        setTimeout(() => {
            document.getElementById('contactForm').style.display = 'none'; // Hide form
            document.getElementById('successMessage').style.display = 'block'; // Show success message

            // Optional: Reset form after a delay (e.g., 3 seconds)
            setTimeout(() => {
                document.getElementById('contactForm').reset();
                document.getElementById('contactForm').style.display = 'block';
                document.getElementById('successMessage').style.display = 'none';
            }, 3000);
        }, 1000); // Simulate 1-second delay for submission
    });
</script>

<style>
    /* Contact wrapper */
    .contact-wrapper {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 1rem;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: 100vh;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Contact header */
    .contact-header-section {
        text-align: center;
        margin-bottom: 2rem;
        background: linear-gradient(135deg, #2d7d46, #4CAF50);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

        .contact-header-section h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .contact-header-section p {
            font-size: 1.1rem;
            color: #6c757d;
            font-weight: 500;
            max-width: 600px;
            margin: 0 auto;
        }

    /* Get in touch section */
    .get-in-touch-section {
        padding: 2rem 1rem;
        text-align: center;
        background: rgba(255, 255, 255, 0.9);
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        margin-bottom: 2rem;
    }

        .get-in-touch-section h2 {
            font-size: 1.8rem;
            font-weight: 700;
            color: #1a5c2e;
            margin-bottom: 1.5rem;
        }

    .contact-form {
        max-width: 800px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(2, 1fr); /* Two equal columns */
        gap: 1.5rem;
        padding: 1rem;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

        .form-group.full-width {
            grid-column: 1 / -1; /* Span both columns */
        }

        .form-group label {
            margin-bottom: 0;
            color: #333;
            font-weight: 600;
            font-size: 0.9rem;
            line-height: 1.2;
        }

        .form-group input[type="text"],
        .form-group input[type="email"],
        .form-group textarea {
            padding: 0.75rem;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            transition: border-color 0.3s ease;
        }

            .form-group input:focus,
            .form-group textarea:focus {
                border-color: #4CAF50;
                outline: none;
                box-shadow: 0 0 0 0.2rem rgba(76, 175, 80, 0.25);
            }

        .form-group textarea {
            resize: vertical;
        }

    .submit-button {
        background: linear-gradient(135deg, #2d7d46, #4CAF50);
        color: white;
        padding: 0.75rem 2rem;
        border: none;
        border-radius: 5px;
        font-size: 1.1rem;
        cursor: pointer;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        grid-column: 1 / -1;
        justify-self: center;
    }

        .submit-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(45, 125, 70, 0.3);
        }

    /* Success Message */
    .success-message {
        text-align: center;
        padding: 1.5rem;
        background-color: #e8f5e9;
        border-radius: 5px;
        border-left: 5px solid #2d7d46;
        margin-top: 1rem;
        display: none;
    }

        .success-message p {
            margin: 0;
            font-size: 1.2rem;
            color: #2d7d46;
            font-weight: 600;
        }

        .success-message i {
            margin-right: 0.5rem;
        }

    /* Contact info section */
    .contact-info-section {
        padding: 2rem 1rem;
        text-align: center;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    }

    .map-container {
        width: 100%;
        max-width: 1200px;
        margin: 0 auto 1.5rem;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

        .map-container iframe {
            height: 400px;
        }

    .contact-details {
        color: #333;
        line-height: 1.8;
        font-size: 1rem;
    }

        .contact-details a {
            color: #1a5c2e;
            text-decoration: none;
            transition: color 0.3s ease;
        }

            .contact-details a:hover {
                color: #4CAF50;
            }

    /* Responsive design */
    @@media (max-width: 768px) {
        .contact-wrapper {
            padding: 1rem 0.5rem;
        }

        .contact-header-section h1 {
            font-size: 1.6rem;
        }

        .get-in-touch-section {
            padding: 1rem 0.5rem;
        }

        .contact-form {
            grid-template-columns: 1fr; /* Stack fields on mobile */
        }

        .map-container iframe {
            height: 300px;
        }

        .submit-button {
            width: 100%;
        }

        .success-message {
            padding: 1rem;
            font-size: 1rem;
        }
    }
</style>